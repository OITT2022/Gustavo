// This component is kept for potential future use with custom meta tags.
// Next.js 15 uses the metadata export pattern instead of <Head>.
// See each page's metadata export for SEO configuration.

export function SeoHead() {
  return null;
}
